# URL of your WordPress site (headless CMS backend).
# Leave unset during development to run entirely on the built-in mock data.
WORDPRESS_URL=https://cms.yourdomain.com

# Optional: address that receives booking inquiries if you are NOT
# routing form submissions into WordPress (see SETUP-WORDPRESS.md).
INQUIRY_NOTIFY_EMAIL=reservations@yourdomain.com
